export interface LedgerVerificationSummary {
  totalAmount: number;
  pendingAmount: number;
  verifiedAmount: number;
  rejectedAmount: number;
  pendingCount: number;
  verifiedCount: number;
  rejectedCount: number;
}
